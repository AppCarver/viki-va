# pytest.ini

[pytest]
# Tell pytest where to look for tests.
# Add the project root to the Python path for import resolution.
pythonpath = .
# Discover tests in specific directories if not following standard patterns
testpaths =
    services
    tests
    shared_libs/utils/tests