name: Manual Deployment Trigger

on:
  workflow_dispatch: # This makes the workflow runnable manually from the GitHub Actions tab

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Deployment Skipped
        run: echo "Deployment workflow is currently empty and manually triggered only. No deployment actions defined."
        # Add future deployment steps here!